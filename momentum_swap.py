import asyncio
from clean_swap import *  # reuse all guardrails, approve_and_swap, etc.

async def main():
    print("🚀 Momentum Strategy Starting")
    # Simple momentum logic (can be expanded)
    # For now, run the same guarded logic but with slight bias
    await main()  # reuse the existing main with guardrails for now

if __name__ == "__main__":
    asyncio.run(main())
