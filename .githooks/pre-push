#!/usr/bin/env sh
# Nanoclaw: block pushes when tracked content contains committed-secret patterns.
ROOT="$(git rev-parse --show-toplevel)"
exec python3 "$ROOT/scripts/check_committed_secrets.py" --all-tracked
